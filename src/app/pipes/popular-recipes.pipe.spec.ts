import { PopularRecipesPipe } from './popular-recipes.pipe';

describe('PopularRecipesPipe', () => {
  it('create an instance', () => {
    const pipe = new PopularRecipesPipe();
    expect(pipe).toBeTruthy();
  });
});
